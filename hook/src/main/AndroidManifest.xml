<?xml version="1.0" encoding="utf-8"?>
<!--
  :hook 库 Manifest。本模块不生成可独立安装的模块 APK，经 library manifest 合入 :app，
  由 :app APK 同时充当 LSPosed 模块。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/hook_module_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/hook_suggested_scope" />
    </application>
</manifest>
